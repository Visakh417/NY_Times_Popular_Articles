import 'package:flutter/material.dart';

class AppColors {
/// Main App Colors will be assigned here.
/// on next app UI modification or color pattern change
/// we have to change color from here only
  static const Color accentColor = Color(0xFFF4D35E);
}
